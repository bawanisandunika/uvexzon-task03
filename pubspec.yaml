name: task03
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  font_awesome_flutter: ^10.5.0
  
  
  # Adding the device_preview package
  device_preview: ^1.1.0  
  firebase_core: ^3.6.0
  firebase_auth: any
  cloud_firestore: ^5.4.4
  google_sign_in: ^6.0.0
  flutter_facebook_auth: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Adding the asset for the welcome screen image
  assets:
    - lib/assets/welcome_image.png
